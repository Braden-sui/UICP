{
    "name": "uicp-root",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "install": "cd uicp && npm install",
        "dev": "cd uicp && npm run tauri:dev",
        "build": "cd uicp && npm run tauri:build"
    }
}
