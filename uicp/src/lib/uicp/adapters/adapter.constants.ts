// Centralized adapter constants
// WHY: Single source of truth for safety caps used across adapter modules.
export const MAX_DATA_COMMAND_LEN = 32768; // 32KB serialized JSON
export const MAX_TEMPLATE_TOKENS = 16; // maximum {{token}} substitutions per element

