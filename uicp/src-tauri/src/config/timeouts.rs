//! Timeout configuration values

// use std::time::Duration;

// Default timeout values in milliseconds (available for future use)
// #[allow(dead_code)]
// pub const DEFAULT_HTTP_TIMEOUT_MS: u64 = 30000; // 30 seconds
// pub const DEFAULT_CONNECT_TIMEOUT_MS: u64 = 5000; // 5 seconds

// Chat completion timeouts
// pub const CHAT_COMPLETION_TIMEOUT_MS: u64 = 120000; // 2 minutes

// Compute operation timeouts
// pub const COMPUTE_TIMEOUT_MS: u64 = 180000; // 3 minutes

// Helper functions to create Duration objects
// pub fn default_http_timeout() -> Duration {
//     Duration::from_millis(DEFAULT_HTTP_TIMEOUT_MS)
// }

// pub fn chat_completion_timeout() -> Duration {
//     Duration::from_millis(CHAT_COMPLETION_TIMEOUT_MS)
// }

// pub fn compute_timeout() -> Duration {
//     Duration::from_millis(COMPUTE_TIMEOUT_MS)
// }
